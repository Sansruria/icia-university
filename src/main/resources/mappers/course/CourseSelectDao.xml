<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="kr.ac.icia.dao.course.CourseSelectDao">

	<select id="getGrade" resultType="FilteringDto2">
		SELECT REQ_GRADE as grade
		FROM COURSE_REQUEST
		WHERE ST_ID = #{stId}
	</select>

	<select id="filtering" resultType="FilteringDto2">
		SELECT REQ_SEMESTER 
		FROM COURSE_REQUEST 
		WHERE ST_ID = #{stId} AND REQ_GRADE = #{grade}
	</select>

</mapper>